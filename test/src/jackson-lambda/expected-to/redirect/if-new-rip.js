// import {
//     expect,
// } from 'chai';
// import lambda from '../../../../../src';

describe('Jackson Lambda', () => {
  describe('Expected to', () => {
    it('Redirect if brand new rip', (done) => {
      // try to delete the rip from rips collection
      // make valid request
      // test Redirect
      // test new rip is in mongo db
      // delete rip from mongo db
      done();
    });
  });
});

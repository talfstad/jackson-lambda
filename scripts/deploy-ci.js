// need to create a version in aws lambda, deploy that. use versioning for a tag
// so i don't have to update a bunch of arns each deployment. use the git status
// to get the version from git, set that as version

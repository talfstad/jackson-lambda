class RequestFinalization {
  static finalizeRequest() {
    return new Promise((resolve) => {
      // TODO: finalize the request.
      // console.log(`Finalizing request ${this}`);
      resolve();
    });
  }
}

export default RequestFinalization;
